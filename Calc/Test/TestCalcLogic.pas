unit TestCalcLogic;
{

  Delphi DUnit Test Case
  ----------------------
  This unit contains a skeleton test case class generated by the Test Case Wizard.
  Modify the generated code to correctly setup and call the methods from the unit 
  being tested.

}

interface

{$RTTI EXPLICIT METHODS([vcPrivate..vcPublished]) PROPERTIES([vcPrivate..vcPublished]) FIELDS([vcPrivate..vcPublished])}

uses
  System.SysUtils, System.Classes, TestFramework, CalcLogic, DUnitTestingCore, TestStructureUnit;

type
  // Test methods for class TCalcLogic

  TestTCalcLogic = class(TCoreTestCase)
  strict private
    TestCalcLogic: TCalcLogic;
  private
    a, b: variant;
    ExpectedResult: variant;
    FailMessage: string;
    Operation: string;
  public
    procedure SetUp; override;
    procedure TearDown; override;
  published
    procedure TestAdd;
    procedure TestSub;
  end;

  TestTCalcLogic1 = class(TCoreTestCase)
  strict private
    TestCalcLogic1: TCalcLogic;
  private
    a, b: integer;
    ExpectedResult: integer;
    FailMessage: string;
    Operation: string;
  public
    procedure SetUp; override;
    procedure TearDown; override;
  published
    procedure TestMul;
    procedure TestDiv;
  end;

implementation

procedure TestTCalcLogic.SetUp;
var
  ParamValue: TInputDataArray;
begin
  TestCalcLogic := TCalcLogic.Create;
  ParamValue := DataArray.Items[Self.FTestName];
  a := ParamValue[0].AsString;
  b := ParamValue[1].AsString;
  ExpectedResult := ParamValue[2].AsString;
  FailMessage := ParamValue[3].AsString;
  Operation := ParamValue[4].AsString;
end;


procedure TestTCalcLogic.TearDown;
begin
  TestCalcLogic.Free;
  TestCalcLogic := nil;
end;


procedure TestTCalcLogic.TestAdd;
var
  ReturnValue: Integer;
begin
  if Operation = 'except' then
  begin
    StartExpectingException(Exception);
    ReturnValue := TestCalcLogic.Add(a, b);
    StopExpectingException('');
    AssertResults<Integer>(ExpectedResult, ReturnValue, Operation, FailMessage);
  end
  else
    AssertResults<Integer>(ExpectedResult, TestCalcLogic.Add(a, b), Operation, FailMessage);
end;


procedure TestTCalcLogic.TestSub;
var
  ReturnValue: Integer;
begin
  if Operation = 'except' then
  begin
    StartExpectingException(Exception);
    ReturnValue := TestCalcLogic.Sub(a, b);
    StopExpectingException('');
    AssertResults<Integer>(ExpectedResult, ReturnValue, Operation, FailMessage);
  end
  else
    AssertResults<Integer>(ExpectedResult, TestCalcLogic.Sub(a, b), Operation, FailMessage);
end;


procedure TestTCalcLogic1.SetUp;
var
  ParamValue: TInputDataArray;
begin
  TestCalcLogic1 := TCalcLogic.Create;
  ParamValue := DataArray.Items[Self.FTestName];
  a := StrToInt(ParamValue[0].AsString);
  b := StrToInt(ParamValue[1].AsString);
  ExpectedResult := StrToInt(ParamValue[2].AsString);
  FailMessage := ParamValue[3].AsString;
  Operation := ParamValue[4].AsString;
end;


procedure TestTCalcLogic1.TearDown;
begin
  TestCalcLogic1.Free;
  TestCalcLogic1 := nil;
end;


procedure TestTCalcLogic1.TestMul;
var
  ReturnValue: Integer;
begin
  if Operation = 'except' then
  begin
    StartExpectingException(Exception);
    ReturnValue := TestCalcLogic1.Mul(a, b);
    StopExpectingException('');
    AssertResults<Integer>(ExpectedResult, ReturnValue, Operation, FailMessage);
  end
  else
    AssertResults<Integer>(ExpectedResult, TestCalcLogic1.Mul(a, b), Operation, FailMessage);
end;


procedure TestTCalcLogic1.TestDiv;
var
  ReturnValue: Integer;
begin
  if Operation = 'except' then
  begin
    StartExpectingException(Exception);
    ReturnValue := TestCalcLogic1.Division(a, b);
    StopExpectingException('');
    AssertResults<Integer>(ExpectedResult, ReturnValue, Operation, FailMessage);
  end
  else
    AssertResults<Integer>(ExpectedResult, TestCalcLogic1.Division(a, b), Operation, FailMessage);
end;

initialization
end.

